[project]
name = "comfyui-conceptsteer"
description = "Concept Steering for ComfyUI — steer image generation with learned concept directions via SAE + contrastive optimization"
version = "0.1.0"
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/nynxz/comfyui-conceptsteer"
Documentation = "https://github.com/nynxz/comfyui-conceptsteer/blob/main/docs/HOW_IT_WORKS.md"

[tool.comfy]
PublisherId = "nynxz"
DisplayName = "Concept Steer"
Icon = ""
Description = "Steer image generation toward learned concepts (cinematic, ethereal, etc.) using SAE/contrastive direction vectors. No LoRA, no fine-tuning — just plug in a lens and adjust strength."
includes = ["js", "docs"]
Repository = "https://github.com/Nynxz/ComfyUI-ConceptSteer"
